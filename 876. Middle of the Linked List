class Solution {
    public ListNode middleNode(ListNode head) {
        // if(head == null) return 0;
        ListNode i = head;
        ListNode j = head;

        while(j != null && j.next != null) {
            i = i.next;
            j = j.next.next;
        }
        return i;

        
    }
}
